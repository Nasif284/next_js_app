import React from 'react'

const Users = async () => {
  // const res = await fetch("http://localhost:3000/users/api/clients");
  // const { users } = await res.json()
  // return (
  //   <>
  //     <div>Users:</div>
  //     {users.map((u: any) => (
  //       <li key={u.id || u.name}>{u.name}</li>
  //     ))}
  //   </>
  // );
}

export default Users